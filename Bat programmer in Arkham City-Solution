//Here we consider the last person and his count, because batman covers all the presons before reaching the last person.

#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,start;
    cout<<"Enter no of patients and Starting posion of first patient";
    cin>>n>>start;
    cout<<"Batman can complete in: "<<n*start<<" Steps";
    return 0;
    
}
